import {Canvas, Controls, Meta, Source, Unstyled} from '@storybook/blocks'
import * as ButtonStories from './ButtonBasic.stories.js'
import TypographyElement from "@/components/ui/TypographyElement.vue";

<Meta of={ButtonStories} />

<Unstyled>
    <TypographyElement variant="h1">Кнопка</TypographyElement>
    <Canvas of={ButtonStories._Button} />
    <TypographyElement variant="h6">Параметры</TypographyElement>
    <Controls />
    <TypographyElement variant="h6">Код компонента</TypographyElement>
    <Source of={ButtonStories._Button} />
    <TypographyElement variant="blockquote" className="mb-6">❗️ Обязательно необходимо указывать для компонента кнопки атрибут
        `type={button | submit | reset}`</TypographyElement>

    <TypographyElement variant="h3" className="mb-6">Главная кнопка (Primary)</TypographyElement>
    <TypographyElement variant="text-main" className="mb-6">Основное действие на экране. Акцентного цвета. Может быть только одна на экране.</TypographyElement>

    <TypographyElement variant="h6">{ButtonStories.Primary.name}</TypographyElement>
    <Canvas of={ButtonStories.Primary} />

    <TypographyElement variant="h6">{ButtonStories.PrimaryDisabled.name}</TypographyElement>
    <Canvas of={ButtonStories.PrimaryDisabled} />

    <TypographyElement variant="h6">{ButtonStories.PrimaryLoading.name}</TypographyElement>
    <Canvas of={ButtonStories.PrimaryLoading} />

    <TypographyElement variant="h3" className="mb-6">Стандартная кнопка (по-умолчанию, default)</TypographyElement>
    <TypographyElement variant="h6" className="mb-4">{ButtonStories.Default.name}</TypographyElement>
    <TypographyElement variant="text-main" className="mb-6">По-умолчанию.</TypographyElement>
    <Canvas of={ButtonStories.Default} />

    <TypographyElement variant="h6" className="mb-6">{ButtonStories.DefaultDisabled.name}</TypographyElement>
    <Canvas of={ButtonStories.DefaultDisabled} />

    <TypographyElement variant="h6" className="mb-6">{ButtonStories.WithLeftIconLoading.name}</TypographyElement>
    <Canvas of={ButtonStories.WithLeftIconLoading} />

    <TypographyElement variant="h6" className="mb-6">{ButtonStories.WithLeftIcon.name}</TypographyElement>
    <TypographyElement variant="text-main" className="mb-6">SVG или другой формат в теге `<img />`. Размеры значка, цвет атрибутов `stroke` и `fill` не задаются, они заданы в самой кнопке.</TypographyElement>
    <Canvas of={ButtonStories.WithLeftIcon} />

    <TypographyElement variant="h6" className="mb-6">{ButtonStories.WithRightIcon.name}</TypographyElement>
    <Canvas of={ButtonStories.WithRightIcon} />

    <TypographyElement variant="h6" className="mb-6">{ButtonStories.OnlyIcon.name}</TypographyElement>
    <TypographyElement variant="blockquote" className="mb-6">❗️ Для кнопок без текста обязательно необходимо указывать атрибут `title='Описание действия'`.</TypographyElement>
    <Canvas of={ButtonStories.OnlyIcon} />

    <TypographyElement variant="h3" className="mb-6">Кнопка третьего порядка (tertiary)</TypographyElement>
    <TypographyElement variant="h6" className="mb-6">{ButtonStories.Tertiary.name}</TypographyElement>
    <Canvas of={ButtonStories.Tertiary} />

    <TypographyElement variant="h3" className="mb-6">Кнопка-ссылка (link)</TypographyElement>
    <TypographyElement variant="text-main" className="mb-6">Обозначение перехода на страницу</TypographyElement>
    <TypographyElement variant="h6" className="mb-6">{ButtonStories.Link.name}</TypographyElement>
    <Canvas of={ButtonStories.Link} />

    <TypographyElement variant="h3" className="mb-6">Кнопка, обозначающая разрушительные действия (destructive)</TypographyElement>
    <TypographyElement variant="h6" className="mb-6">{ButtonStories.Destructive.name}</TypographyElement>
    <Canvas of={ButtonStories.Destructive} />

    <TypographyElement variant="h6" className="mb-6">{ButtonStories.DestructiveLoading.name}</TypographyElement>
    <Canvas of={ButtonStories.DestructiveLoading} />
</Unstyled>